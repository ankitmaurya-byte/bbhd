import React from "react";
const folderPath = "../../public/"; // Replace with the actual path to your folder
import * as path from "path";
// const useSampleImages = () => {
//  const [images, setImages] = React.useState<string[]>([]);

//  try {
//    const files = fs.readdirSync(folderPath);
//    const imageNames = files.filter((file) => {
//      const extension = path.extname(file).toLowerCase();
//      return [".jpg", ".jpeg", ".png", ".gif"].includes(extension);
//    });

//    console.log(`Number of images: ${imageNames.length}`);
//    console.log("Image names:", imageNames);
//  } catch (error) {
//    console.error("Error reading folder:", error);
//  }

// };

// export default useSampleImages;
